{
  "name": "officebridge-ssh-client",
  "version": "0.1.0",
  "private": true,
  "description": "Local TCP-forwarding client for OfficeBridge's tcp:// (SSH/RDP/DB) services — a stand-in for the eventual Bridge-app UI integration.",
  "main": "index.js",
  "dependencies": {
    "ws": "^8.16.0"
  }
}
